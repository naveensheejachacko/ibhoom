services:
  # Backend API Service
  - type: web
    name: ibhoom-backend
    env: python
    plan: free
    rootDirectory: backend
    buildCommand: |
      pip install -r requirements.txt
      python init_db.py
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./marketplace.db
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: JWT_ACCESS_TOKEN_EXPIRE_MINUTES
        value: 30
      - key: BACKEND_CORS_ORIGINS
        value: https://ibhoom-frontend.onrender.com,http://localhost:5173,http://localhost:3000,http://localhost:5174
      - key: DEBUG
        value: false

  # Frontend Node.js Web Service (serves React SPA)
  - type: web
    name: ibhoom-frontend
    env: node
    plan: free
    rootDirectory: frontend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_URL
        value: https://ibhoom.onrender.com
